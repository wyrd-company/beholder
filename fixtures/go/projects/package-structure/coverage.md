---
relationships:
  references:
    - ground-rules
    - package-structure
    - reports/go/synthesis/checklist
---

# Package structure coverage

| Canonical checklist identifier | Stable source path | Named declaration, directive, package, module, or test | Build context | Additional variant locators |
| --- | --- | --- | --- | --- |
| GO-CAN-SRC-001 | `doc.go`, `declarations.go`, `imports_default.go`, `external_test.go` | `package pantry` across the library files; `package pantry_test` in `external_test.go`; `BeforeDeclaration` calls `AfterDeclaration` declared later | Default | `imports_default.go` and `package_identity.go` use separate import sets; `external_test.go` is a package-clause contrast with no behavioral test |
| GO-CAN-SRC-002 | `imports_default.go`, `package_identity.go`, `measure/amount.go`, `labels/bright/label.go`, `labels/dim/label.go`, `cmd/showcase/main.go` | Module `example.com/fixture/package-structure`; package `quantity` in directory `measure`; package `label` in both label directories; `package main` in `cmd/showcase` | Default | `quantity` is imported by its default name and an explicit alias; `brightLabel` and `dimLabel` distinguish same-named packages; the command path has a `main` clause |
| GO-CAN-SRC-003 | `scopes.go`, `universe.go`, `labels.go` | `scopeParameters`, `nestedScopes`, `shadowedUniverse`, and `labelDance` | Default | Parameter and named-result scopes are in `scopeParameters`; nested block shadowing is in `nestedScopes`; labels use a separate function namespace |
| GO-CAN-SRC-004 | `universe.go` | `unshadowedUniverse`, `shadowedUniverse`, `acceptsComparable` | Default (`go 1.26`; supports identifiers introduced in Go 1.18 and Go 1.21) | `any`, `comparable`, `clear`, `min`, `max`, `len`, `true`, and `nil` are shadowed locally while unshadowed calls and constraints remain in `unshadowedUniverse` and `acceptsComparable` |
| GO-CAN-SRC-005 | `exported.go`, `labels/bright/label.go`, `labels/dim/label.go`, `internal/notes/note.go` | `Shelf`, `shelf`, `PublicValue`, `privateValue`, `PublicFunction`, `privateFunction`, `Shelf.Open`, `Shelf.close`, `Étage`, and `πράγμα` | Default | `Shelf` contains exported and unexported fields; both `label` packages contain distinct unexported `tone` members; `notes.Token` is exported from an internal package |
| GO-CAN-SRC-006 | `blank.go`, `blank_import.go` | `blankAssignments`, `blankReturns`, `blankRange`, `Parcel`, and compile-time assertion `var _ shelfOpener = Shelf{}` | Default | Blank import, assignments, blank named results, blank struct field, range index/value, and interface assertion each have a stable locator |
| GO-CAN-SRC-007 | `labels.go` | `labelDance` | Default | `label`, `breakLabel`, and `finish` are labels; `label` is also a same-spelled variable in the separate label namespace; the closure has its own label scope |
| GO-CAN-SRC-008 | `unused.go` | `unusedPackageConstant`, `unusedPackageType`, `unusedPackageVariable`, `unusedParameters`, and `suppressedLocal` | Default | Package declarations, an unused parameter/result, `_ = value` suppression, and an assignment-only local are all valid source |
| GO-CAN-SRC-009 | `positions.go`, `doc.go` | `numericLiterals`, `automaticSemicolons`, `LogicalDirectiveValue`, `BlockDirectiveValue`, and the package/declaration comments | Default | `//line logical-source.go:17`, `/*line block-source.go:29*/`, the ordinary near-miss `// line ...`, digit separators, binary/hex/octal literals, and newline semicolon insertion |
| GO-CAN-IMP-001 | `imports_default.go`, `imports_alias.go`, `dot_import.go`, `blank_import.go` | File-scoped import declarations in `defaultImportExample`, `aliasImportExample`, `dotImportExample`, and `blankImportedAnchor` | Default | Default-name, explicit-alias, dot, and blank forms occur in separate files and retain file-local bindings |
| GO-CAN-DEC-001 | `declarations.go` | Grouped and ungrouped declarations `GroupedCount`, `LooseCount`, `GroupedRecord`, `LooseRecord`, `BeforeDeclaration`, `AfterDeclaration`, `mutualLeft`, and `mutualRight` | Default | `Earlier` aliases `Later` before its textual declaration; package declarations are split across files while local lexical scope remains in `short.go` |
| GO-CAN-DEC-002 | `initialization_values.go`, `initialization.go`, `init_secondary.go` | `directSource`, `directValue`, `throughValue`, `independentValue`, `cycleLeft`, `cycleRight`, `recordInitialization`, and the local `init` functions | Default | Direct and through-function dependencies cross files and append to `initTrace`; independent state is separate; `cycleRight` is assigned from `cycleLeft` in `init` after its zero-value initialization |
| GO-CAN-DEC-003 | `registry/registry.go`, `branches/morning/morning.go`, `branches/evening/evening.go`, `blank_import.go`, `initialization.go`, `init_secondary.go` | Multiple root `init` functions; `registry.init`; `InitializationTrace` | Default | Root imports `morning` and `evening`, both depend on `registry`, and root also blank-imports `registry`, forming a diamond with one registrar initialization |
| GO-CAN-DEC-005 | `short.go` | `shortDeclaration`, `partialRedeclaration`, and `initializerScope` | Default | New-plus-existing names, multi-value expressions, nested shadowing, parameter shadowing, and initializer scope each use a non-blank new name in their block |
| GO-CAN-DEC-006 | `local_types.go` | `localNamedTypes`, `localPoint`, `PackagePoint`, and `PackagePointAlias` | Default | Local defined type identity and conversion contrast with a package-defined equivalent and an alias; no invalid local method declaration is included |
