# behavior-suite coverage

| Canonical identifier | Stable source path | Named coverage source | Build context | Additional locators |
| --- | --- | --- | --- | --- |
| GO-CAN-TST-001 | `parcel_internal_test.go` | `package parcel`, `TestUnexportedCounter`, and `TestPackageInitialization` use unexported declarations and test-only imports | Test build | `parcel_external_test.go`: `package parcel_test`, `TestExternalPackage`, and imports of `parcel` and `support` |
| GO-CAN-TST-002 | `parcel_internal_test.go` | `TestMain`, `TestPackageInitialization`, and `BenchmarkCounter` provide test-main, initialization, test, and benchmark discovery declarations | Test build | `parcel_external_test.go`: `Example`, `ExampleCounter`, `ExampleAdd`, and `ExampleCounter_Increment`; `fuzz-legacy/fuzz_test.go`: `FuzzMirror`; `testHelper` is an ordinary non-test helper |
| GO-CAN-TST-003 | `parcel_internal_test.go` | `TestSubtestLifecycle` uses nested `Run`, dynamic names, `Cleanup`, closure capture, and `Parallel` | Test build | `BenchmarkCounter`: `increment` and `snapshot` sub-benchmarks |
| GO-CAN-TST-004 | `parcel_external_test.go` | `Example`, `ExampleCounter`, `ExampleAdd`, and `ExampleCounter_Increment` bind examples to the package, type, function, and method with ordered output | Test build | `ExampleAdd_unordered` uses unordered output; `ExampleCounter_documentation` is a no-output documentation example; all examples use external `parcel_test` package form |
| GO-CAN-TST-005 | `fuzz-legacy/fuzz_test.go` | `FuzzMirror` calls `F.Add` and `F.Fuzz` with a supported `string` argument | Test and fuzz builds | `fuzz-legacy/go.mod`: `go 1.17`; `fuzz-legacy/testdata/fuzz/FuzzMirror/seed.txt`: checked-in seed corpus |
| GO-CAN-TST-006 | `parcel_internal_test.go` | `TestPackageRelativeTestdata` reads package-relative `testdata/notes.txt` and `testdata/invalid.go` | Test build | `testdata/invalid.go` is intentionally invalid Go but excluded from package walks; `testdata/notes.txt` is non-Go data |
| GO-CAN-TST-007 | `vetfixtures/printf.go` | `printfTarget` is a legal printf analyzer target; standalone `go vet -tags vetfixtures ./...` also visits `structtag.go`, `copylocks.go`, `buildtag.go`, `lostcancel.go`, and `loopclosure.go`; standalone `go vet -buildtag=false -tags vettests ./...` visits `tests_test.go` | Standalone vet with `vetfixtures` and `vettests` tags; `vetfixtures/go.mod` uses `go 1.21` for version-sensitive `loopclosure` | `vetfixtures/quiet.go`: quiet near-neighbor; required lint disables only `buildtag` for its intentional mismatch; required test uses the curated analyzer list without `buildtag`, and all tagged analyzer targets stay outside normal package/test builds |
| GO-CAN-MOD-011 | `parcel.go` | Recursive `Node` type and `Sum` call form a valid recursive package graph | Default and test builds | `support/bridge.go` imports `parcel`; `parcel_external_test.go` uses external `parcel_test` and imports both `parcel` and `support`, avoiding a test-configuration cycle |
