# `pipeline-scheduler` coverage

| Canonical identifier | Stable source path | Coverage declaration, directive, package, module, or test | Build context | Additional variant locators |
| --- | --- | --- | --- | --- |
| GO-CAN-CON-001 | `pipeline/worker.go` | `LaunchExamples`, `namedWorker`, `methodWorker.run`, `evaluatedArgument` | default | Closure loop-capture boundary in `LaunchExamples`; panic isolation in `LaunchRecoveredPanic`; join and deterministic collection in `LaunchExamples` |
| GO-CAN-CON-002 | `pipeline/channels.go` | `BufferedChannelSnapshot`, `DirectionalExchange`, `NilChannelBlocks`, `NilChannelDisabled`, `ClosedChannelPanics` | default | Buffered capacity, close/range/comma-ok in `BufferedChannelSnapshot`; send-only and receive-only boundaries in `sendDirectional` and `receiveDirectional`; closed send and double close recovery in `ClosedChannelPanics` |
| GO-CAN-CON-003 | `pipeline/selects.go` | `SelectSnapshotExample`, `ReadySelectChoice`, `EmptySelectBlocks` | default | Send, receive, default, nil disabling, and expression effects in `SelectSnapshotExample`; simultaneously ready closed channels in `ReadySelectChoice` |
| GO-CAN-CON-004 | `pipeline/synchronization.go` | `SynchronizedState.PublishOverChannel`, `PublishByClose`, `ProtectWithMutex`, `InitializeOnce`, `JoinWorkers`, `AtomicSnapshot`, `BufferedCapacityBoundary`, `UnsynchronizedState` | default | Channel send and close, mutex, once, wait group, typed atomic, buffered-capacity nuance, and unsynchronized read/write contrast |
| GO-CAN-CON-005 | `racecase/race.go` | `RaceExample`, `SynchronizedExample`, `UnsafeCounter`, `SafeCounter` | default and `-race` | `racecase/race_enabled.go` and `racecase/race_disabled.go` select the `race` build tag; `RaceExample` is the true race and `SynchronizedExample` is its mutex-neighbor |
| GO-CAN-CON-006 | `pipeline/atomics.go` | `TypedCounter.TypedOperations`, `LegacyAtomicOperations`, `LegacyLayout`, `CopyAtomicWrapper` | default | Typed load/store/CAS in `TypedCounter`; legacy operations and first-word alignment in `LegacyAligned`; 386 alignment declaration in `pipeline/alignment_386.go`; wrapper-copy boundary in `CopyAtomicWrapper` |
| GO-CAN-DYN-005 | `pipeline/lifecycle.go` | `CallbackRegistry`, `NewFinalizableResource`, `UseFinalizableResource`, `NewCleanupResource`, `StopCleanup`, `DropResource` | default | Deterministic callback retention/cancellation in `CallbackRegistry`; finalizer and `KeepAlive` in `FinalizableResource`; versioned `runtime.AddCleanup` and `runtime.Cleanup.Stop` in `CleanupResource` |
| GO-CAN-DYN-009 | `pipeline/context_errors.go` | `NewCancellableContext`, `NewDeadlineContext`, `CancellationStateOf`, `BuildErrorChain`, `InspectError`, `NotifyCancellation`, `LoseCancellation`, `DetachCancellation` | default | Sentinel identity/message comparison in `ErrorIdentity`; typed, wrapped, joined, custom `Is`, `As`, and `Unwrap` in `StageError`, `CodeError`, `JoinedError`; callback timing in `NotifyCancellation`; cancellation cause and lost cancellation in context helpers |
| GO-CAN-DIA-009 | `pipeline/optimization.go` | `MapIterationOrder`, `ReadySelectChoice`, `SchedulingOrder`, `FinalizerObservation`, `InitializationTrace`, `IndependentArguments`, `EscapingBuffer`, `InlineCandidate`, `InterfaceReduction`, `GenericInstantiations`, `DeadCodeBoundary` | default and `-race` | Race instrumentation in `racecase/race_enabled.go`; map/select/scheduling observations remain open; independent init values, escape, inlining candidate, interface devirtualization candidate, generic instantiations, and dead branch are all portable boundaries |

