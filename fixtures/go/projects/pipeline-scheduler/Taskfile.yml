version: '3'

tasks:
  build:
    cmds:
      - go build ./...
      - go build -race ./...
      - env GOARCH=386 go build ./...

  lint:
    cmds:
      - test -z "$(gofmt -l .)"
      - go vet ./...

  test:
    cmds:
      - go test ./...

