package identity

// ModulePath is the declared module identity rather than a physical directory.
const ModulePath = "example.invalid/module-federation"

// PackagePath demonstrates the package path derived from ModulePath.
const PackagePath = ModulePath + "/pkg/identity"
