# Module federation coverage

| Canonical identifier | Stable source path | Named coverage | Build context | Additional locators and valid variants |
| --- | --- | --- | --- | --- |
| GO-CAN-MOD-001 | `go.mod` | `module example.invalid/module-federation`, `go 1.24`, and direct requirements | Module mode | `pkg/identity/identity.go`: `ModulePath`, `PackagePath`; `pkg/pathshape/shape.go`: package name differs from its directory name; `deps/legacy-module/go.mod`: older module metadata with no `go` directive; `pkg/*` import paths derive from the module identity. |
| GO-CAN-MOD-002 | `go.mod` | `go` and `toolchain go1.26.5` directives | `GOTOOLCHAIN=local` | `versioned/newer.go`: `go1.25` release-constrained file selected by the installed release; `Taskfile.yml`: local-toolchain commands. |
| GO-CAN-MOD-003 | `mvs-probe/go.mod` | Direct root requirements and selected indirect `graphdep v1.2.0` requirement | Module mode, local replacements | `deps/graph-root-low/go.mod`: transitive `graphdep v1.0.0`; `deps/graph-root/go.mod`: transitive `graphdep v1.2.0`; `mvs-probe/probe.go`: both roots resolve to `deps/graphdep-high/version.go`; `pkg/graph/graph_test.go`: test-only `testsupport` import; root `go.mod`: selected high version retained for vendor consistency. |
| GO-CAN-MOD-004 | `go.mod` | Coexisting `example.invalid/semver v1.4.0` and `/v2 v2.3.0` requirements | Module mode, local replacements | `pkg/federation/federation.go`: aliases `semver` and `semverv2`; `deps/semver-v1/label` and `deps/semver-v2/label`: same package name under distinct module identities. |
| GO-CAN-MOD-005 | `go.mod` | Main-module `replace` and `exclude` directives | Module mode, local replacements | `deps/replacement-consumer/go.mod`: dependency-local replacement; `deps/replacement-local/target`: main replacement result; `deps/retracted/go.mod`: `retract v1.0.0`; `pkg/federation/federation.go`: `consumer.Value` and `notice.Status`. |
| GO-CAN-MOD-006 | `go.work` | Workspace `use` set and workspace-level beta replacement | Workspace on and off | `workspace/alpha/bridge`: alpha imports beta; `workspace/beta/api`: beta imports alpha; `workspace/beta-local` is the off-workspace replacement; `Taskfile.yml`: workspace and `GOWORK=off` builds. |
| GO-CAN-MOD-007 | `nested-module/go.mod` | Nested module boundary beneath the parent project | Module mode and workspace | `go.mod` requires/replaces `example.invalid/nested`; `nested-module/feature/feature.go` is reached when wired; `src/plain/plain.go` is an ordinary subdirectory without a nested module. |
| GO-CAN-MOD-008 | `internal/visibility/visible.go` | Legal parent-tree import of a nested `internal` package | Default module context | `internal/visibility/internal/leaf/leaf.go`: nested internal element and exported `Marker`; `pkg/federation/federation.go`: legal import from the parent tree; `privateMarker`: exportedness is separate from path visibility. |
| GO-CAN-MOD-009 | `vendor/modules.txt` | Workspace vendor metadata and selected dependency packages | `-mod=vendor` and `-mod=mod` | `vendor/example.invalid/*`: vendored local dependency source; `Taskfile.yml`: both vendor and module-mode commands. |
| GO-CAN-MOD-010 | `go.sum` | Hermetic checksum/source policy for local replacements and vendor resolution | `GOPROXY=off`, `GOSUMDB=off` | `go.sum` records checksums for `graphdep` v1.0.0 and v1.2.0; `proxy/example.invalid/graphdep/@v` is the checked-in local proxy; `vendor/modules.txt` and `go.mod` replacement directives provide canonical offline resolution. |
| GO-CAN-MOD-012 | `legacy/legacy.go` | Preserved canonical import-path comment on `package legacy` | Module mode | `package legacy // import "example.invalid/module-federation/legacy"` is inert under module loading. |
| GO-CAN-MOD-013 | `go.mod` | Go 1.24 `tool` directive for `example.invalid/toolkit/cmd/forge` | `go tool`, local module resolution | `tools/tools.go`: pre-1.24 build-tagged blank import; `deps/toolkit/cmd/forge/main.go`: tracked tool package; `Taskfile.yml`: no runtime import edge. |
| GO-CAN-ECO-001 | `cmd/inspect/main.go` | Multiple command packages and tool-significant layout directories | Default module and workspace contexts | `cmd/serve`; `internal/visibility`; `vendor`; `testdata`; `nested-module`; `pkg` and `src` are ordinary names; `Taskfile.yml` builds each declared module. |
| GO-CAN-ECO-002 | `pkg/stdlib/stdlib.go` | `math/rand` and `math/rand/v2` standard packages coexist with aliases | Installed Go 1.26 standard library | `legacyrand` and `randv2` imports share declared package name `rand` while retaining distinct standard-library identities and APIs. |
